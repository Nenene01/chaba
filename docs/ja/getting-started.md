# Chabaを始める

## 前提条件

- **Git**: バージョン2.5以降 (`git worktree`サポート用)
- **GitHub CLI** (`gh`): PR操作用
- **Node.js** または **Rust**: 実装に応じて (未定)

## インストール

> **注意**: Chabaは現在開発初期段階です。インストール方法は近日公開予定です。

### 予定されているインストール方法

```bash
# Homebrew経由 (macOS/Linux)
brew install chaba

# Cargo経由 (Rust)
cargo install chaba

# npm経由 (Node.js)
npm install -g chaba

# ソースから
git clone https://github.com/Nenene01/chaba.git
cd chaba
cargo build --release  # または: npm run build
```

## 初期セットアップ

### 1. GitHubで認証

```bash
gh auth login
```

### 2. Chabaを設定

設定ファイルを作成:

```bash
# グローバル設定
mkdir -p ~/.config/chaba
chaba config init

# またはプロジェクト固有
cd your-project
chaba config init --local
```

`~/.config/chaba/chaba.yaml` を編集:

```yaml
worktree:
  base_dir: "~/reviews"  # レビューworktreeを作成する場所
  auto_cleanup: true
  keep_days: 7

sandbox:
  auto_install_deps: true

agents:
  default_review_agents: ["claude"]
```

## 基本的な使い方

### プルリクエストをレビュー

```bash
# プロジェクトに移動
cd ~/projects/myapp

# PR #123のレビューを開始
chaba review --pr 123
```

**実行されること:**
1. PRブランチをfetch
2. `~/reviews/pr-123` にworktreeを作成
3. 依存関係をインストール
4. レビュー環境のパスを出力

**出力:**
```
✓ PR #123を取得: "Add user authentication"
✓ ~/reviews/pr-123 にworktreeを作成
✓ 依存関係をインストール (npm install)
✓ レビュー準備完了!

レビューを開始するには:
  cd ~/reviews/pr-123
  code .  # または好みのエディタ
```

### ブランチ名でレビュー

```bash
chaba review --branch feature/new-api
```

### AIエージェント分析を有効化

```bash
# デフォルトエージェント (Claude Code) を使用
chaba review --pr 123 --with-agent

# 詳細レビューのため複数エージェントを使用
chaba review --pr 123 --thorough
```

### テストツール付きデバッグモード

```bash
# Playwright統合で起動
chaba debug --pr 123 --with-playwright

# APIデバッグツールで起動
chaba debug --pr 123 --with-api-debug
```

## レビュー環境で作業

レビュー環境がセットアップされたら:

```bash
# レビューworktreeに移動
cd ~/reviews/pr-123

# テストを実行
npm test  # または: cargo test

# 開発サーバーを起動 (セットアップ時に割り当てられたポート)
npm run dev  # サーバーは割り当てられたポート (例: 3042) で起動

# 必要に応じて変更を加える
# メインワークスペース ~/projects/myapp には影響なし
```

## クリーンアップ

### レビュー環境を削除

```bash
# PR #123のレビューをクリーンアップ
chaba cleanup --pr 123
```

**実行されること:**
- worktreeを削除
- sandbox環境をクリーンアップ
- 割り当てられたポートを解放

### アクティブなレビューをリスト表示

```bash
chaba list
```

**出力:**
```
アクティブなレビュー環境:
  PR #123  ~/reviews/pr-123  feature/new-api     (2日前)
  PR #456  ~/reviews/pr-456  bugfix/auth-error   (5時間前)
```

### 自動クリーンアップ

設定で `auto_cleanup: true` の場合、古いレビュー環境は `keep_days` 後に自動的に削除されます。

## 高度な使い方

### カスタムWorktree場所

```bash
chaba review --pr 123 --worktree ~/custom/path
```

### 依存関係のインストールをスキップ

```bash
chaba review --pr 123 --no-deps
```

### 特定のエージェントを使用

```bash
chaba review --pr 123 --agent codex
```

### 並行レビュー

```bash
# 複数のPRを同時にレビュー
chaba review --pr 123 &
chaba review --pr 456 &
```

各レビューは独自のworktreeとポート割り当てを取得します。

## トラブルシューティング

### "Worktree already exists"

```bash
# 既存のworktreeを先に削除
chaba cleanup --pr 123

# または --force で置き換え
chaba review --pr 123 --force
```

### "Port already in use"

Chabaは自動的に次の利用可能なポートを割り当てます。必要に応じて設定の `port_range_start` を確認してください。

### "Failed to fetch PR"

認証されていることを確認:
```bash
gh auth status
```

### AIエージェントが応答しない

エージェントログを確認:
```bash
chaba logs --pr 123 --agent claude
```

## 次のステップ

- 高度な設定については [設定ガイド](configuration.md) を参照
- Chabaの仕組みについては [アーキテクチャ](architecture.md) を参照
- Chabaの改善に協力するには [CONTRIBUTING.md](../../CONTRIBUTING.md) を確認

---

**楽しいレビューを! 🍵**
